CREATE TABLE USERS
(
	ID			SERIAL PRIMARY KEY,
    FIRSTNAME   VARCHAR(100),
    LASTNAME    VARCHAR(100)
);

COMMIT;

SELECT * FROM USERS;

CREATE TABLE ISSUESLISTS
(
	ID          SERIAL PRIMARY KEY,
    NAME   		VARCHAR(100)
);

INSERT INTO ISSUESLISTS (NAME)
VALUES ("ToDo"),("In Progress"),("Done");

SELECT * FROM ISSUESLISTS;

CREATE TABLE ISSUES
(
	ID       			SERIAL PRIMARY KEY,
    ISSUESLIST_ID		BIGINT UNSIGNED,
    SUMMARY				VARCHAR(100),
    DESCRIPTION			VARCHAR(1024),
    USER_ID_ASSIGNEDTO	BIGINT UNSIGNED,
    
    FOREIGN KEY (ISSUESLIST_ID) REFERENCES ISSUESLISTS(ID),
    FOREIGN KEY (USER_ID_ASSIGNEDTO) REFERENCES USERS(ID)
);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (1, "Something is broken", "Sink is leaking but no visible crack/defect", 2);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (1, "Something is broken", "Also the tap is leaking, Zacharov please do something about it.", 2);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (1, "Power outrage", "Server234 is not waking up", 4);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (3, "Power outrage", "Server233 is not waking up", 4);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (2, "Power outrage", "Server236 is not waking up", 4);

SELECT * FROM ISSUES;

COMMIT;